import NormalizedMessage = require('../NormalizedMessage');
declare const _default: () => (message: NormalizedMessage, useColors: boolean) => string;
/**
 * Creates new default formatter.
 *
 * @returns {defaultFormatter}
 */
export = _default;
